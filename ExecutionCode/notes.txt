The control wrapper knows what its state is, how to control components, and how to execute logical steps.

The process knows what steps to do in what order.

The process handler knows how to start and stop processes.

The UI knows how to command the process handler, including starting processes and controlling individual components, and how to ask it for the control wrapper's state.